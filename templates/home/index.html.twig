{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}

    <ul>
        {% for article in articles %}
            <li>
                {{ article.name }}
                {{ component('ArticleFav', {article:article}) }}
            </li>
        {% endfor %}
    </ul>

{% endblock %}
